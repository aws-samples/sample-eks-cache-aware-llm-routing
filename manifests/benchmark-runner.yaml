apiVersion: v1
kind: Pod
metadata:
  name: benchmark-runner
  namespace: inference
spec:
  containers:
    - name: python
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      resources:
        requests:
          cpu: "1"
          memory: 2Gi
  restartPolicy: Never
